apiVersion: v1
kind: Service
metadata:
  name: binarycalculatorwebapp-service
  labels:
    app: binarycalculatorwebapp
spec:
  type: LoadBalancer
  selector:
    app: binarycalculatorwebapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
